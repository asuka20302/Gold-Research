# Generated outputs

Model outputs, figures, cached models and full prediction ledgers are generated
locally and ignored by Git. Run the applicable script from the repository root
to recreate them. Small final tables intended for code review are copied to
`reports/`.
